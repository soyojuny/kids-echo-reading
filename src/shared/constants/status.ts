export const BOOK_INPUT_STATUS = ["empty", "draft", "ready"] as const;
export const BOOK_STATUS = ["draft", "ready", "archived"] as const;
export const PAGE_TTS_ASSET_STATUS = ["pending", "ready", "failed"] as const;
export const READING_ATTEMPT_STATUS = ["uploaded", "assessed", "failed"] as const;
